#!/bin/bash

set -eux

source /var/vcap/packages/golang-1.13-linux/bosh/compile.env
mkdir -p ${BOSH_INSTALL_TARGET}/bin
mkdir -p ${GOPATH}/src

mv streaming-mysql-backup-client ${GOPATH}/src

go build -o ${BOSH_INSTALL_TARGET}/bin/streaming-mysql-backup-client streaming-mysql-backup-client
